import { DeepFilterPipe } from './deep-filter.pipe';

describe('DeepFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DeepFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
