import { OnlyAlphaDirective } from './only-alpha.directive';

describe('OnlyAlphaDirective', () => {
  it('should create an instance', () => {
    const directive = new OnlyAlphaDirective();
    expect(directive).toBeTruthy();
  });
});
